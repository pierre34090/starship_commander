--- src/App.jsx
+++ src/App.jsx
@@ -1,4 +1,5 @@
     } else if (newEnemyHp <= 0) {
       setEnemy(null);
-      setScrap((prev) => prev + 5); // reward scrap
-      setLastResult(`✅ Victory! You destroyed ${enemy.name}`);
+      const reward = 5;
+      setScrap((prev) => prev + reward); // reward scrap
+      setLastResult(`✅ Victory! You destroyed ${enemy.name} and collected ${reward} scrap.`);